import { definePlaceholder } from '../core/placeholder.js';

definePlaceholder(
  's02-landscape',
  2,
  'What is What',
  'Model, Product, and API — the three concepts, the many-to-many map, and the DB filter view of what is actually allowed.'
);
