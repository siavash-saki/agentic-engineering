import { definePlaceholder } from '../core/placeholder.js';

definePlaceholder(
  's03-gap',
  3,
  'The Capability Gap',
  'The same simple-looking question, the same failure. Having access to a model is not the same as getting value from it.'
);
