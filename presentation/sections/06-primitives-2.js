import { definePlaceholder } from '../core/placeholder.js';

definePlaceholder(
  's06-primitives-2',
  6,
  'Primitives — Hooks, Subagents, Scope',
  'The remaining building blocks plus project-level vs user-level scope. The five primitives together as a single matrix.'
);
