import { definePlaceholder } from '../core/placeholder.js';

definePlaceholder(
  's05-primitives-1',
  5,
  'Primitives — Memory, Skills, MCP',
  'Three framework-agnostic building blocks, each mapped to its Copilot equivalent so the concepts land concretely.'
);
